'use strict';

describe('myApp.television module', function() {

  beforeEach(module('myApp.television'));

  describe('television controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var televisionCtrl = $controller('TelevisionCtrl');
      expect(televisionCtrl).toBeDefined();
    }));

  });
});